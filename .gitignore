# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# NOTE: .terraform.lock.hcl is intentionally NOT ignored.
# It MUST be committed for reproducible provider builds.

# Environment variable overrides (local only)
*.auto.tfvars
*.auto.tfvars.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Secrets — never commit
.env
.env.*
